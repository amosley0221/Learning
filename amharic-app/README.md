# Amharic Learning

A Progressive Web App (PWA) for learning Amharic. Self-contained single-page
application served as a static site.

## Deploy

Configured for Render via `render.yaml`. Push to GitHub, then on
dashboard.render.com choose **New + → Blueprint** and point at the repo.
